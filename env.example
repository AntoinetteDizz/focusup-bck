# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=focusup

# Application Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# OpenAI Configuration (if using AI features)
OPENAI_API_KEY=your_openai_api_key_here

# Render specific variables
# Add these in your Render dashboard:
# DB_HOST=your_render_postgres_host
# DB_PORT=5432
# DB_USERNAME=your_render_postgres_username
# DB_PASSWORD=your_render_postgres_password
# DB_DATABASE=your_render_postgres_database
# NODE_ENV=production
# CORS_ORIGIN=your_frontend_url

# SSL Configuration (automatically handled in production)
# The application will automatically enable SSL when NODE_ENV=production 